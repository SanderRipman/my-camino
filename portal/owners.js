const SUPABASE_URL='https://ibloovohuhrceivrvhvn.supabase.co';const SUPABASE_KEY='sb_publishable_JtNmgzTLlepPhKDCVsn6CA_Vk7BCClv';const client=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);const $=s=>document.querySelector(s);let session=null,participants=[],context=null;
function esc(v=''){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
async function cmd(action,body={}){return client.functions.invoke('case-command',{body:{action,...body}})}
async function init(){const {data:{session:s}}=await client.auth.getSession();session=s;if(!session){location.replace('./');return}const aal=await client.auth.mfa.getAuthenticatorAssuranceLevel(),ok=aal.data?.currentLevel==='aal2';$('#securityPill').textContent=ok?'AAL2 · bekreftet':'AAL1 · utilstrekkelig';$('#securityPill').classList.toggle('secure',ok);if(!ok){$('#blocked').classList.remove('hidden');return}const {data,error}=await client.from('participants').select('id,code_name,stage,updated_at').eq('active',true).order('updated_at',{ascending:false});participants=data||[];if(error||!participants.length){$('#blocked').classList.remove('hidden');return}$('#participantSelect').innerHTML=participants.map(p=>`<option value="${p.id}">${esc(p.code_name)} · ${esc(p.stage)}</option>`).join('');const q=new URLSearchParams(location.search).get('participant');if(q&&participants.some(p=>p.id===q))$('#participantSelect').value=q;const probe=await loadContext();if(!probe){$('#blocked').classList.remove('hidden');return}$('#work').classList.remove('hidden')}
function peopleOptions(rows,current){return '<option value="">Velg ansvarlig</option>'+rows.map(p=>`<option value="${p.user_id}" ${p.user_id===current?'selected':''}>${esc(p.full_name)}${p.job_title?` · ${esc(p.job_title)}`:''}</option>`).join('')}
async function loadContext(){const participantId=$('#participantSelect').value;if(!participantId)return false;const {data,error}=await cmd('LIST_CONTEXT',{participantId});if(error||data?.error)return false;context=data;$('#stage').textContent=data.participant?.stage||'–';$('#pilot').textContent=data.pilot?.name||'Ikke tildelt';$('#viaOwner').innerHTML=peopleOptions(data.eligible?.via||[],data.assessment?.owner_user_id);$('#vidaOwner').innerHTML=peopleOptions(data.eligible?.vida||[],data.assessment?.vida_owner_user_id);$('#viaMessage').textContent=data.assessment?.owner_user_id?'VÍA-eier er navngitt.':'VÍA-eier er ikke navngitt ennå.';$('#vidaMessage').textContent=data.assessment?.vida_owner_user_id?'VIDA-eier er navngitt.':'VIDA-eier mangler – SER kan ikke startes.';return true}
async function save(action,selectId,messageId){const participantId=$('#participantSelect').value,targetUserId=$(selectId).value;if(!targetUserId){$(messageId).textContent='Velg en ansvarlig først.';return}$(messageId).textContent='Lagrer…';const {data,error}=await cmd(action,{participantId,targetUserId});if(error||data?.error){$(messageId).textContent='Eierskapet kunne ikke lagres med valgt rolle/scope.';return}$(messageId).textContent='Ansvar lagret og revisjonsført.';await loadContext()}
$('#participantSelect').addEventListener('change',loadContext);$('#saveVia').addEventListener('click',()=>save('SET_VIA_OWNER','#viaOwner','#viaMessage'));$('#saveVida').addEventListener('click',()=>save('SET_VIDA_OWNER','#vidaOwner','#vidaMessage'));client.auth.onAuthStateChange(()=>setTimeout(init,0));init();