# Public candidate QA – 2026-08-18

Source: `/mnt/data/mobile_candidate`, derived from exact Netlify production deploy `6a818fb0563075817cf6ffdd` plus controlled microfixes.

## Static integrity

All 9 HTML pages passed:
- every local link/script/stylesheet/image reference resolves inside the candidate package;
- exactly one H1 per page;
- footer present on every page;
- `site.js?v=20260818a` present on every page;
- no duplicate IDs found;
- every image has an alt attribute;
- buttons/links have an accessible text name;
- corrected `site.js` does not use iframe-inflated `document.documentElement.scrollHeight` as its height source.

Pages checked: `index`, `via`, `ser`, `vida`, `deltakere`, `partnere`, `ruter`, `om`, `kontakt`.

## Source-derived mobile height reference

A print-layout renderer was used only as a temporary sizing reference for the current static source while the live child still reports a polluted iframe viewport height. Approximate footer-bottom positions:

| Page | 320 px | 390 px | 412 px |
|---|---:|---:|---:|
| index | 7869 | 7621 | 7589 |
| via | 3825 | 3600 | 3545 |
| ser | 4067 | 3849 | 3817 |
| vida | 3988 | 3748 | 3745 |
| deltakere | 5386 | 5103 | 5063 |
| partnere | 4065 | 3825 | 3823 |
| ruter | 8368 | 8263 | 8260 |
| om | 3583 | 3394 | 3358 |
| kontakt | 2508 | 2403 | 2400 |

These values are **not** the permanent architecture. Wix custom embed revision 6 temporarily interpolates source-derived mobile heights so the known bad live child report cannot keep the page at the old oversized iframe height. Remove that fallback once the corrected child `site.js` is deployed and verified.

## Release gate

Still required before public Git-source cutover:
1. reconcile/import binary WebP assets into the Git deployment source or explicitly restore them during build;
2. deploy corrected child source to a non-production preview;
3. mobile QA at 320/360/390/412 px and desktop QA;
4. verify footer stop, internal navigation top, SER → VIDA, VÍA charging callout, language switch and my.AidMe link;
5. retain deploy `6a818fb0563075817cf6ffdd` as rollback until post-release verification.
